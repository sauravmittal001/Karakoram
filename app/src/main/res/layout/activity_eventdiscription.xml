<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#EAE3B8"
    android:orientation="vertical">





            <TextView
                android:id="@+id/event_name"
                android:layout_width="match_parent"
                android:text="event name"
                android:layout_marginTop="15dp"
               android:layout_marginLeft="100dp"
                android:layout_height="wrap_content"
                android:layout_marginBottom="10dp"
                android:textSize="25dp" />

            <TextView
                android:id="@+id/event_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="this event is about................."
                android:layout_marginLeft="30dp"
                android:textSize="20dp"
                android:layout_marginBottom="20dp"/>

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="350dp"
        android:src="@drawable/user_image"
        android:id="@+id/event_image"
       />




        <TextView
            android:id="@+id/event_time"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="time"
            android:layout_marginBottom="25dp"
            android:layout_marginLeft="300dp"
            android:layout_marginTop="25dp"


            />


    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/event_details"
        android:text="In android, Adapter will act as an intermediate between the data sources and adapter views such as ListView, Gridview to fill the data into adapter views. The adapter will hold the data and iterates through items in the data set and generate the views for each item in the list.
Generally, adapter views, those are
" />

</LinearLayout>